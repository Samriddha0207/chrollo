{
  "name": "chrollo",
  "version": "1.4.0",
  "private": true,
  "description": "Repository security review workspace for HackSpire",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server/app.js",
    "start": "node server/app.js",
    "build": "npm run check",
    "test": "node --test",
    "check": "node --check api/index.js && node --check server/ai.js && node --check server/app.js && node --check server/env.js && node --check server/external-scanners.js && node --check server/github.js && node --check server/jobs.js && node --check server/repository.js && node --check server/scanners.js && node --check server/service.js && node --check server/store.js && node --check server/utils.js && node --check dist/app.js"
  },
  "dependencies": {
    "tar": "7.5.22"
  }
}
